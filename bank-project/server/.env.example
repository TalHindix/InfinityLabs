PORT=3000
NODE_ENV=development
SERVER_URL=http://localhost:3000
MONGO_URI=mongodb://localhost:27017/Dubai-Bank
# Generate JWT_SECRET with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=1h
BREVO_API_KEY=
EMAIL_FROM=noreply@example.com
OPENAI_API_KEY=
CLIENT_URL=http://localhost:5173
# When frontend and API are on different origins (e.g. Vercel + Render), set so the auth cookie is sent cross-origin:
# COOKIE_SAME_SITE=none
