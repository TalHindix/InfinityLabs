// Route paths used for navigation and routing; single source of truth.
export const ROUTES = {
  LOGIN: '/login',
  SIGNUP: '/signup',
  DASHBOARD: '/dashboard',
  TRANSFER: '/transfer',
  TRANSACTIONS: '/transactions',
} as const;
